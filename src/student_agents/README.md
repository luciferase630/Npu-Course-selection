# student_agents

未来放学生投豆策略，包括效用比例策略、必修惩罚优先策略、公式信息组策略和大模型代理策略。

所有策略的最终输出必须是非负整数投豆。若策略内部产生连续建议值，应先取整、截断到可用预算，并再次校验总投豆不超预算。

学生代理可以使用课程班容量、剩余容量和当前待选人数判断竞争强度，但不能读取其他学生的投豆数或投豆分布。

轮内动态策略需要支持 `keep`、`increase`、`decrease`、`withdraw`、`new_bid` 五类动作。策略可以在截止前多次修改投豆，但最终输出给开奖模块的只能是截止时的非负整数投豆。
